@model List<ListViewModel>
<div class="col-lg-12">
    <div class="card">
        <div class="card-header">
            <h4 class="card-title">Navbar</h4>
        </div><!--end card-header-->
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped mb-0">
                    <thead>
                        <tr>
                            <th scope="col">@Html.DisplayNameFor(b=>b.GetEnumerator().Current.Id)</th>
                            <th scope="col">@Html.DisplayNameFor(b=>b.GetEnumerator().Current.Name)</th>
                            <th scope="col">@Html.DisplayNameFor(b=>b.GetEnumerator().Current.Order)</th>

                            <th class="text-end">Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var navbar in Model)
                        {
                            <tr>
                                <td>@navbar.Id</td>
                                <td>@navbar.Name</td>
                                <td>@navbar.Order</td>
                                <td>
                                    <a asp-route="admin-navbar-update" asp-route-id="@navbar.Id"><i class="las la-pen text-secondary font-16"></i></a>
                                    <form method="POST" asp-route="admin-navbar-delete" asp-route-id="@navbar.Id" id="navbar-form-@navbar.Id"></form>
                                    <button type="submit" form="navbar-form-@navbar.Id">
                                        <i class="las la-trash-alt text-secondary font-16"></i>
                                    </button>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table><!--end /table-->
            </div><!--end /tableresponsive-->
        </div><!--end card-body-->
    </div><!--end card-->
</div>