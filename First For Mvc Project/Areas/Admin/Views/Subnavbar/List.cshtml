@model List<ListViewModel>
<div class="col-lg-12">
    <div class="card">
        <div class="card-header">
            <h4 class="card-title">Navbar</h4>
        </div><!--end card-header-->
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped mb-0">
                    <thead>
                        <tr>
                            <th scope="col">@Html.DisplayNameFor(b=>b.GetEnumerator().Current.Id)</th>
                            <th scope="col">@Html.DisplayNameFor(b=>b.GetEnumerator().Current.Name)</th>
                            <th scope="col">@Html.DisplayNameFor(b=>b.GetEnumerator().Current.NavbarName)</th>

                            <th class="text-end">Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var SubNavbar in Model)
                        {
                            <tr>
                                <td>@SubNavbar.Id</td>
                                <td>@SubNavbar.Name</td>
                                <td>@SubNavbar.NavbarName</td>
                                <td>
                                    <a asp-route="admin-subnavbar-update" asp-route-id="@SubNavbar.Id"><i class="las la-pen text-secondary font-16"></i></a>
                                    <form method="POST" asp-route="admin-subnavbar-delete" asp-route-id="@SubNavbar.Id" id="navbar-form-@SubNavbar.Id"></form>
                                    <button type="submit" form="navbar-form-@SubNavbar.Id">
                                        <i class="las la-trash-alt text-secondary font-16"></i>
                                    </button>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table><!--end /table-->
            </div><!--end /tableresponsive-->
        </div><!--end card-body-->
    </div><!--end card-->
</div>